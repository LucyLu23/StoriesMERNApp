export default (posts6=[], action) =>{
    switch (action.type) {
        case 'FETCH_ALL':
            return posts6;

        case 'CREATE':
            return posts6;

        default:
            return posts6;
    }
}